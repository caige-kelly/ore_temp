b :: @import("build.ore")

build :: pub fn(void) -> void
    exe := b.add_executable(.{
        .name = "allocator_test", 
        .root = b.root(.{
            .source_file = b.path("main.ore"),
            .target = .APPLESILICON,
        }),
    })

    builder.install_artifact(exe)
