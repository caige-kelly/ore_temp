allocator :: @import("allocator.ore")


main :: fn(void) -> i32
    comptime if (@build.mode == .debug) 
        allocator.debug 
    else 
        allocator.another_one

    r := alloc(u8, 1024)
    defer free(r)

    0