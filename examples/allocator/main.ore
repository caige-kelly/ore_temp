allocator :: @import("./allocator.ore")
a :: allocator.Allocator


main :: fn() i32

    comptime if (@build.mode == .debug) 
        allocator.debug
    else 
        allocator.release 

    a.alloc()

    0