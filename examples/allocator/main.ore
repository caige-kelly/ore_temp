allocator :: @import("allocator")
exn       :: @import("exn")


main :: fn() i32
    with allocator.default

    comptime if (@build.mode == .debug) 
        allocator.debug 
    else 
        allocator.another_one

    handle_try() 

    0