allocator :: @import("allocator.ore")
exn       :: @import("exn.ore")

main :: fn(void) i32
    comptime if (@build.mode == .debug)
        with allocator.debug
        with exn.verbose
    else
        with allocator.system
        with exn.fast

    r := alloc(u8, 1024)
    defer free(r)

    0