allocator :: @import("./allocator.ore")


main :: fn() i32
    with allocator.debug

    comptime if (@build.mode == .debug) 
        allocator.debug
    else 
        allocator.Allocator

    handle_try() 

    0