allocator :: @import("./allocator.ore")
builtin   :: @import("builtin")
a :: allocator.Allocator


main :: fn() i32
    with comptime if (builtin.mode == builtin.Mode.debug) 
        allocator.debug
    else 
        wallocator.release 

    a

    0