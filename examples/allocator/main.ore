allocator :: @import("allocator.ore").Allocator

main :: fn(void) i32
    comptime if (@build.mode == .debug)
        with @build.handlers.exn
        with @build.handlers.allocator

    r1 := allocator.alloc(u8, 1024)
    defer allocator.free(r1)

    0