allocator :: @import("./allocator.ore")
builtin   :: @import("builtin")


foo :: struct
  hello : []const u8

main :: pub fn() i32
    comptime if (builtin.mode == .debug)
        with allocator.debug
    else
        with allocator.release

    r1 := a.alloc(u8, 1024)


    return 0