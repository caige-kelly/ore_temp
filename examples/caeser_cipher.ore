// -------------------------------------
// Constants
// -------------------------------------

EXIT_FAILURE : i32 : 1
EXIT_SUCCESS : i32 : 0

english : [26; f64] : .{
    8.2, 1.5, 2.8, 4.3, 12.7, 2.2,
    2.0, 6.1, 7.0, 0.2, 0.8, 4.0, 2.4,
    6.7, 7.5, 1.9, 0.1, 6.0, 6.3, 9.1,
    2.8, 1.0, 2.4, 0.2, 2.0, 0.1
}

// -------------------------------------
// Exn Effect
// -------------------------------------

effect :: exn
  throw :: |comptime t: type, e: t| -> void

exn :: |action: |void| -> void| -> void
  with handler
    throw :: |t, e|
      @stackTrace()
      stderr("panic: {s}.{s}\n", .{ @typeName(t), @tagName(e) })
      exit(EXIT_FAILURE)
      
  action()

// -------------------------------------
// Console Effect
// -------------------------------------

conosle :: effect
  stdout :: |comptime fmt: []const u8, args: [...]| -> void

console :: |action: |void| -> void| -> void
  with handler
    stdout :: |void| 
      //do some formatting
      ```
        // asm
      ```

// ------------------------------------
// Allocator effect definition
// -------------------------------------

MallocError :: enum
  OutOfMemory

malloc :: effect 
  alloc :: |comptime t: type, count: usize| -> [*]t

malloc :: |action: |void| -> void| -> void <exn>
  with handler
    alloc :: |comptime t: type, count: usize| -> [*]t
      raw : [*]void = ore_alloc(sizeof(t) * count, @alignOf(t))

      if raw == @null then
        throw(MallocError, .OutOfMemory)

      @ptrcast(raw)

  action()

// -------------------------------------
// Helpers
// -------------------------------------

mod26 :: |x: i32| -> i32
    r : i32 = x % 26
    if r < 0 then r + 26 else r

is_lower :: |c: u8| -> bool
    c >= 'a' && c <= 'z'

// -------------------------------------
// Encoding
// -------------------------------------

caesar :: |text: []const u8, shift: i32, buf: []u8| -> void

    for c: u8, i: isize in text
        if is_lower(c) then
            base : u8 = c - 'a'
            rot : i32 = mod26(@intCast(base) + shift)
            buf[i] <- @intCast(rot) + 'a'
        else
            buf[i] <- c

// -------------------------------------
// Frequency analysis
// -------------------------------------

freqs :: |text: []const u8| -> [26; f64]
    counts : [26; i32] = .{0}
    total : i32 = 0

    for c: u8 in text where is_lower(c)
        idx : u8 = c - 'a'
        counts[idx] <- counts[idx] + 1
        total++

    result : [26; f64] = .{0.0}

    if total == 0 then
        return result

    for j: isize in 0..26
        result[j] <- 100.0 * (@floatcast(counts[j]) / @floatcast(total))

    result

// Rotate frequency table by n
rotate :: |xs: [26; f64], n: i32| -> [26; f64]
    result : [26; f64] = .{0.0}

    for i: i32 in 0..26
        j : isize = @intCast(mod26(i + n))
        result[i] <- xs[j]

    result

// Chi-square statistic
chisqr :: |xs, ys: [26; f64]| -> f64
    sum : f64 = 0.0

    for i: isize in 0..26
        x : f64 = xs[i]
        y : f64 = ys[i]
        d : f64 = x - y
        sum += (d * d) / y

    sum

// -------------------------------------
// Crack Caesar (find best shift)
// -------------------------------------

crack :: |text: []const u8, buf: []u8| -> void
    table : [26; f64] = freqs(text)

    best_shift : i32 = 0
    best_score : f64 = F64_MAX

    for n: i32 in 0..26
        rotated : [26; f64] = rotate(table, n)
        score : f64 = chisqr(rotated, english)

        if score < best_score then
            best_score <- score
            best_shift <- n

    // decoding = encoding with negative shift
    caesar(text, 0 - best_shift, buf)

// -------------------------------------
// Main
// -------------------------------------

main :: |void| -> i32
    with .{ exn, console, malloc } 

    plain : []const u8 : "khoor ruh lv d ixq odqjxdjh"

    enc : [*]u8 = alloc(u8, plain.len)

    caesar(plain, 3, enc[0..plain.len]) 

    dec: [*]u8 = alloc(u8, plain.len)

    crack(enc[0..plain.len], dec[0..plain.len])

    stdout("plain  : {s}\n", .{plain})
    stdout("encoded: {s}\n", .{enc[0..plain.len]})
    stdout("cracked: {s}\n", .{dec[0..plain.len]})

    EXIT_SUCCESS
