Shape :: struct 
  tag: enum { Circle; Square; }
  union 
    circle :: struct
        diameter: i32
    square :: struct 
        length: i32
        width: i32

area :: |s: Shape| -> i32
  switch s.tag
    .Circle => s.circle.diameter + s.circle.diameter
    .Square => s.square.length + s.square.width





