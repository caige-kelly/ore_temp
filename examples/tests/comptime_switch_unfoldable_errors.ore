/*
    Negative — `comptime switch` scrutinee must fold at compile time.

    Expected diag (1):
    - comptime switch scrutinee must be comptime-known
*/

probe :: pub fn(runtime_arg: i32) i32
    comptime switch (runtime_arg)
        0 => 1
        _ => 2
